# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- Data layer ----
SQLAlchemy[asyncio]==2.0.36
alembic==1.14.0
aiosqlite==0.20.0          # dev / local
aiomysql==0.2.0            # MySQL async driver (production target)

# ---- Validation / settings ----
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# ---- Security ----
PyJWT==2.10.1
argon2-cffi==23.1.0
cryptography==44.0.0       # at-rest encryption for connection tokens

# ---- Infra clients ----
redis==5.2.1               # optional; in-memory fallback when unset
httpx==0.28.1
orjson==3.10.13

# ---- Billing ----
stripe==11.4.1

# ---- Dev / test ----
pytest==8.3.4
pytest-asyncio==0.25.2
